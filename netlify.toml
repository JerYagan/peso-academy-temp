[build]
  command = "npm run build"
  publish = "dist"

# Specify Node.js version (optional but recommended)
[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

